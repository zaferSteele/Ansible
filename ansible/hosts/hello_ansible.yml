- name: Ansible Playbook for printing "Hello, Ansible!"
  hosts: localhost
  gather_facts: false

  tasks:
  - name: Print "Hello, Ansible!"
    ansible.builtin.debug:
      msg: "Hello, Ansible!"
    