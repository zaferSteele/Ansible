- name Configure Cisco IOS-XE routers interfaces
  hosts: routers_type1
  gather_facts: false 
  tasks:
  - name: Configure R4 and R5 g0/1 interfaces on 1.1.45.0/30
    cisco.ios.ios_command:
      commands:
      - 